"""Future parsing functions for documented ClinVar release formats."""


def parse_clinvar_release() -> None:
    """Parse one ClinVar release after its schema has been verified."""
    raise NotImplementedError(
        "Parsing requires a chosen ClinVar format and a documented field mapping."
    )
