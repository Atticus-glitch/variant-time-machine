# Notebooks

Use notebooks for documented exploration and visualization, not as the only copy of pipeline logic. Move reusable transformations into `src/variant_time_machine/`, record the input release and cutoff date, restart and run notebooks cleanly before sharing, and do not commit large embedded outputs or private information.
